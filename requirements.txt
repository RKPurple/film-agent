psycopg2-binary>=2.9,<3.0
chromadb>=0.5,<1.0
sentence-transformers>=3.0,<4.0
rank-bm25>=0.2,<1.0
snowballstemmer>=2.2,<3.0
# Pinned exactly: cinemagent/agent.py imports the Interactions client's error
# classes from a private SDK module (google.genai._gaos.lib.compat_errors), so
# upgrades must be deliberate -- rerun pytest and a live chat check after any bump.
google-genai==2.20.0
python-dotenv>=1.0,<2.0
